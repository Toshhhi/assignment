# MongoDB Connection String
# For local development, you can use MongoDB Atlas (free tier) or local MongoDB
MONGODB_URI=mongodb://localhost:27017/frontend-intern-task
# Or use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/frontend-intern-task?retryWrites=true&w=majority

# JWT Secret Key (change this to a random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-characters

# Next.js Public API URL (optional, defaults to /api)
NEXT_PUBLIC_API_URL=/api

